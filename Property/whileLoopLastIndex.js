/* let text = "The rain in Spain stays mainly in the plain";
const pattern = /ain/g;
while (pattern.test(text) === true) {
  console.log("pattern ", pattern.test(text)); //===================== last index for while loop =============//
  console.log(pattern);
}
 */
